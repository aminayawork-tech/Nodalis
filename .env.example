# Postgres connection string (Prisma Postgres, Vercel Postgres, Neon, Supabase, or your own).
#
# If you're on Supabase and deploying to Vercel (or any serverless host), use
# the "Session pooler" connection string (same pooler host, port 5432), not
# the direct one. Supabase's direct connection is IPv6-only, which Vercel
# can't reach; the pooler is IPv4. Use Session mode rather than Transaction
# mode (port 6543) — Prisma Migrate needs session-level locking that
# Transaction-mode pooling doesn't support and will hang instead of erroring.
# (Other providers, e.g. Neon/Prisma Postgres, don't have this gotcha — their
# direct connection strings work fine from Vercel.)
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# Anthropic API key, used for narrative synthesis. Get one at https://console.anthropic.com
ANTHROPIC_API_KEY=

# Firecrawl API key, used to scrape source articles for a topic. Get one at https://www.firecrawl.dev
# Leave unset to fall back to a stub scraper (returns no sources; synthesis will note it can't verify details).
FIRECRAWL_API_KEY=

# SerpApi key, used for the "Trending Now" dashboard panel and the daily
# refresh job's trend discovery (SerpApi's Google Trends Trending Now
# engine). Get one at https://serpapi.com. Leave unset to fall back to a
# Firecrawl+Claude approximation of "what's trending."
SERPAPI_API_KEY=

# Shared secret required (as ?secret=... or Authorization: Bearer ...) to call /api/cron/refresh.
CRON_SECRET=changeme
